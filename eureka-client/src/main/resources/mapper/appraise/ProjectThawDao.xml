<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >
<mapper namespace="com.zpepdi.eureka_client.dao.appraise.ProjectThawDao">
    <insert id="addProjectThaw" useGeneratedKeys="true" keyProperty="map.id">
        insert into project_thaw(project_id, handler, handle_time, reason, audit)
        values(#{map.projectId}, #{userId}, now(), #{map.reason}, 0)
    </insert>
</mapper>